<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.carlospinan.mueveloconandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnimationsExampleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.BallExampleActivity" />
        <activity android:name=".activities.FlipExampleActivity" />
        <activity android:name=".activities.KeyFrameExampleActivity" />
        <activity android:name=".activities.RocketExampleActivity" />
        <activity android:name=".activities.SpriteAnimationExampleActivity" />
        <activity android:name=".activities.sharedElement.SharedOneExampleActivity" />
        <activity android:name=".activities.sharedElement.SharedTwoExampleActivity" />
    </application>

</manifest>